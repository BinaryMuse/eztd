$("#tasks #task_block_<%= @task.id %>").remove();
<% content = escape_javascript render('task', :task => @task) %>
$('<%= @task.completed? ? "#completed" : "#uncompleted" %>').append("<%= content %>");
