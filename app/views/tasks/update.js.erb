el = $("#tasks #task_block_<%= @task.id %>");
if(el.hasClass("completed")) {
  el.removeClass("completed");
  el.addClass("uncompleted");
} else {
  el.removeClass("uncompleted");
  el.addClass("completed");
}
