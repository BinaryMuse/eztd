<% unless @task.valid? %>

  alert("Task cannot be empty.");

<% else %>

  $("#tasks").append("<%= escape_javascript render('task', :task => @task) %>");
  $("#task_block_<%= @task.id %>").effect('highlight', 500);
  $("#task_text").val("");
  $("#no_tasks").hide();

<% end %>